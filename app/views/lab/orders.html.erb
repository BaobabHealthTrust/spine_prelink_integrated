<html>
  <head>
    <title>Current Orders</title>
    <style type="text/css">
      body {
        font-family: "Nimbus Sans L","Arial Narrow",sans-serif;
      }
    </style>
  </head>
  <body>
    <table width="100%" cellpadding="10" cellspacing="1" style="border-left: 1px solid #6281A7;">
      <tr style="font-size: 14px; background-color: #6281A7; color: #fff; font-weight: bold;">

        <% @fields.each do |field| %>

          <td style="text-align: center; vertical-align: top;">
            <%= field.gsub(/timestamp/, "Date Retrieved").gsub(/_/," ").titleize rescue "" %>
          </td>

        <% end %>

      </tr>

      <% @orders.each do |order| %>

        <tr>
          <% @fields.each do |field| %>

            <td style="border-right: 1px solid #6281A7; border-bottom: 1px solid #6281A7; vertical-align: top;">
              <%= order[1][field] %>&nbsp;
            </td>

          <% end %>

        </tr>

      <% end %>

    </table>
  </body>
</html>